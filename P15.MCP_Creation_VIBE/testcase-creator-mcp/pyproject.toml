[project]
name = "testcase-creator-mcp"
version = "1.0.0"
description = "MCP server exposing a VWO manual QA test-case dataset as Tools, Resources and Prompts."
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "fastmcp==2.14.7",
]

[tool.uv]
package = false
